# Directory
src/ProjectL_frontend/src/pages2/HomePage

# Included files
- index.jsx
- index.scss
- components/BannerSkeleton.jsx
- components/BannerSkeleton.scss
- components/DesktopBannerSection.jsx
- components/DesktopGameSection.jsx
- components/DesktopHomeContent.jsx
- components/GameSection.jsx
- components/GamesSkeleton.jsx
- components/GamesSkeleton.scss
- components/desktop.scss
- components/slick.css
- imgs/Vector.svg

==================================================
FILE: src/ProjectL_frontend/src/pages2/HomePage/index.jsx
==================================================

import React, { useEffect, useState } from "react";
import { useNavigate } from "react-router-dom";
import { useHomeData } from "@/api/home";
import { Swiper } from "@nutui/nutui-react";
import GameSection from "./components/GameSection";
import BannerSkeleton from "./components/BannerSkeleton";
import GamesSkeleton from "./components/GamesSkeleton";
import DesktopHomeContent from "./components/DesktopHomeContent";
import "./index.scss";

const SwiperItem = Swiper.Item;
const DESKTOP_BREAKPOINT = 768;

const HomePage = () => {
  const { banners, games, gamesLoading, bannersLoading } = useHomeData();
  const navigate = useNavigate();
  const [windowWidth, setWindowWidth] = useState(() => {
    if (typeof window === "undefined") return 0;
    return window.innerWidth;
  });

  const isDesktop = windowWidth >= DESKTOP_BREAKPOINT;
  useEffect(() => {
    const handleResize = () => {
      setWindowWidth(window.innerWidth);
    };

    handleResize();
    window.addEventListener("resize", handleResize);

    return () => {
      window.removeEventListener("resize", handleResize);
    };
  }, []);

  useEffect(() => {
    const html = document.documentElement;
    const className = "home-desktop-full-width";

    if (isDesktop) {
      html.classList.add(className);
    } else {
      html.classList.remove(className);
    }

    return () => {
      html.classList.remove(className);
    };
  }, [isDesktop]);

  const onBannerClick = (banner) => {
    console.log('onBannerClick: ', banner);
    if (!banner.linkUrl) return;
    let aEl;
    switch(banner.linkType) {
      case 0: 
        navigate(banner.linkUrl)
        break;
      case 1: {
        aEl = document.createElement('a');
        aEl.href = banner.linkUrl;
        aEl.target = '_blank';
        aEl.click();
      }
    }
  };

  const onBannerKeyDown = (event, banner) => {
    if (event.key !== "Enter" && event.key !== " ") return;
    event.preventDefault();
    onBannerClick(banner);
  };

  const handleGameClick = (game) => {
    if (game.status === "coming_soon") return;

    localStorage.setItem('currentGameInfo', JSON.stringify({
      productId: game.id,
      productName: game.name,
      productLogo: game.logo,
      route: game.route
    }));

    if (game.frontend_route) {
      navigate({ pathname: game.frontend_route }, {
        state: {
          productId: game.id,
          productName: game.name,
          productLogo: game.logo
        }
      });
      return;
    }

    navigate({ pathname: game.frontend_route || game.route, search: `?pid=${game.id}` }, {
      state: {
        productId: game.id,
        productName: game.name,
        productLogo: game.logo
      }
    });
  };

  return (
    <div className={`home-page relative${isDesktop ? " desktop-mode" : ""}`}>
      {/* 顶部Logo和社区按钮 - 立即显示 */}
      <div className="home-header bg-gradient-to-r from-[#D5D6F7] via-[#E1E2F9] to-[#D3C3FB] border-b border-purple-200/30 shadow-sm">
        <div className="home-header-content">
          <div className="logo-area">
            <div className="logo"></div>
          </div>
          <span className="title">Randseed</span>
          <a href="/community/wltoken" target="_blank" className="community-btn"><span></span>Community</a>
        </div>
      </div>

      {isDesktop ? (
        <DesktopHomeContent
          banners={banners}
          games={games}
          bannersLoading={bannersLoading}
          gamesLoading={gamesLoading}
          onBannerClick={onBannerClick}
          onGameClick={handleGameClick}
        />
      ) : (
        <>
          {/* Banner轮播 - 数据加载完成前显示骨架屏 */}
          {bannersLoading ? (
            <BannerSkeleton />
          ) : banners && banners.length > 0 ? (
            <div className="home-banner">
              <Swiper
                loop={banners.length > 1}
                autoPlay={banners.length > 1 ? 5000 : 0}
                indicator
                style={{
                  "--nutui-swiper-indicator-bottom": "12rem"
                }}
              >
                {banners.map((banner, index) => (
                  <SwiperItem key={banner.id}>
                    <img
                      onClick={() => onBannerClick(banner)}
                      className="banner-image"
                      src={banner.imageUrl}
                      alt={banner.title || "Randseed banner"}
                      loading={index === 0 ? "eager" : "lazy"}
                      fetchpriority={index === 0 ? "high" : "auto"}
                      decoding="async"
                      role={banner.linkUrl ? "button" : undefined}
                      tabIndex={banner.linkUrl ? 0 : undefined}
                      onKeyDown={(event) => onBannerKeyDown(event, banner)}
                      style={{
                        cursor: banner.linkUrl ? "pointer" : "default"
                      }}
                    />
                  </SwiperItem>
                ))}
              </Swiper>
            </div>
          ) : null}

          {/* 游戏区域 - 数据加载完成前显示骨架屏 */}
          <div className="home-games">
            {gamesLoading ? (
              <>
                <GamesSkeleton title="Rand Game" />
                <GamesSkeleton title="Rand Ball" />
              </>
            ) : (
              games.map((game, index) => {
                return (
                  <GameSection
                    key={index}
                    title={game[0]} 
                    games={game[1]} 
                  />
                );
              })
            )}
          </div>
        </>
      )}
    </div>
  );
};

export default HomePage;

==================================================
FILE: src/ProjectL_frontend/src/pages2/HomePage/index.scss
==================================================

.home-page {
  background: 
    linear-gradient(177deg, rgba(255, 255, 255, 0.00) 7.99%, #fff 100%),
    linear-gradient(272deg, #D5D6F7 1.78%, #E1E2F9 46.59%, #D3C3FB 99.68%);
  background-size: 100% 314rem, 100% 314rem;
  background-repeat: no-repeat;
  padding-bottom: 60rem;
  min-height: calc(min(100dvh, 100vh) - 60rem);
  padding-top: 68rem;
  font-family: var(--app-font-family);

  &.desktop-mode {
    font-family: var(--app-font-family);
  }
  
  .loading-container {
    display: flex;
    justify-content: center;
    align-items: center;
    height: 200rem;
    margin-top: 76rem;
    
    .loading-text {
      font-size: 16rem;
      color: #666;
    }
  }
  
  .home-header {
    position: fixed;
    top: 0;
    left: 0;
    right: 0;
    z-index: 100;
    
    .home-header-content {
      display: flex;
      justify-content: space-between;
      align-items: center;
      width: 100%;
      max-width: var(--max-content-width-pc);
      margin: 0 auto;
      padding: 12rem 16rem;
    }
    
    .logo-area {
      display: flex;
      align-items: center;
      margin-right: 45rem;
      
      .logo {
        display: inline-block;
        width: 40rem;
        height: 40rem;
        margin-right: 8rem;
        background: url('/images/logo.png') no-repeat center;
        background-size: contain;

      }
    }

    .title {
      font-size: 16rem;
      font-weight: bold;
      letter-spacing: 0.5rem;
      color: #000;
      text-align: center;
    }
    
    .community-btn {
      width: 85rem;
      height: 24rem;
      flex-shrink: 0;
      border-radius: 20rem;
      border: 1rem solid #000;
      color: #000;
      font-family: inherit;
      font-size: 10rem;
      font-weight: 500;
      display: flex;
      justify-content: center;
      align-items: center;
      gap: 3rem;
      span {
        display: inline-block;
        height: 14rem;
        width: 14rem;
        background: url('./imgs/Vector.svg') no-repeat center;
        background-size: contain;
      }
    }

    @media (min-width: 768px) {
      .title {
        font-size: 20rem;
      }
      .community-btn {
        width: 145rem;
        height: 40rem;
        font-size: 15rem;
        border: 2rem solid #000;
        gap: 6rem;
        span {
          width: 18rem;
          height: 18rem;
        }
      }
      .logo-area {
        margin-right: 105rem;
      }
    }
  }
  
  .home-banner {
    margin: 0 16rem 20rem 16rem;
    border-radius: 12rem;
    overflow: hidden;
    height: 160rem;
    
    .nut-indicator {
      display: flex;
      gap: 3rem;
      justify-content: center;
      
      .nut-indicator-dot {
        width: 14rem;
        height: 3rem;
        border-radius: 8rem;
        background: rgba(255, 255, 255, 0.5);
        transition: all 0.3s ease;
        
        &.nut-indicator-active {
          background: #FFFFFF!important;
        }
      }
    }
    
    .banner-image {
      width: 100%;
      height: 160rem;
      border-radius: 0rem !important;
      display: block;
      object-fit: cover;
    }
    
    .banner-link {
      display: block;
      width: 100%;
      height: 160rem;
      text-decoration: none;
      
      &:hover {
        text-decoration: none;
      }
    }
    
    .banner-placeholder {
      width: 100%;
      height: 160rem;
      background: linear-gradient(135deg, #667eea 0%, #764ba2 100%);
      display: flex;
      align-items: center;
      justify-content: center;
      position: relative;
      
      .banner-content {
        text-align: center;
        color: white;
        z-index: 2;
        
        h3 {
          font-size: 14rem;
          margin: 0rem 0rem 6rem 0rem;
          opacity: 0.9;
        }
        
        h2 {
          font-size: 24rem;
          font-weight: bold;
          margin: 0rem 0rem 12rem 0rem;
          text-shadow: 1rem 1rem 2rem rgba(0,0,0,0.3);
        }
        
        .banner-stats {
          display: flex;
          gap: 20rem;
          justify-content: center;
          
          .stat {
            text-align: center;
            
            .stat-label {
              font-size: 10rem;
              opacity: 0.8;
              margin-bottom: 2rem;
            }
            
            .stat-value {
              font-size: 16rem;
              font-weight: bold;
            }
          }
        }
      }
    }
    
    .banner-img {
      width: 100%;
      height: 160rem;
      object-fit: cover;
      border-radius: 12rem;
      display: block;
    }
  }
  
  .game-section {
    margin: 0rem 16rem 24rem 16rem;
    
    &:first-of-type {
      margin-top: 20rem;
    }

    .game-list {
      display: block;
    }
    
    .section-title {
      font-size: 18rem;
      font-weight: 600;
      margin: 0rem 0rem 10rem 0rem;
      color: #000;
    }
    
    .game-item {
      display: flex;
      align-items: flex-start;
      border-radius: 12rem;
      margin-bottom: 11rem;
      transition: background 0.2s;
      gap: 6rem;
    
      &.coming-soon {
        opacity: 0.65;
        
        .play-btn {
          background: #eee;
          color: #aaa;
          cursor: default;
        }

        .game-status {
          color: rgba(0, 0, 0, 0.45);
        }
      }
      
      .game-logo {
        width: 52rem;
        height: 52rem;
        border-radius: 10rem;
        margin-right: 6rem;
        background: #fff;
        display: flex;
        align-items: center;
        justify-content: center;
        overflow: hidden;
        position: relative;
        
        .game-logo-img {
          width: 100%;
          height: 100%;
          object-fit: cover;
          border-radius: 10rem;
        }
        
        .game-logo-placeholder {
          width: 100%;
          height: 100%;
          border-radius: 10rem;
          background: linear-gradient(135deg, #4CAF50, #2196F3);
          display: flex;
          align-items: center;
          justify-content: center;
          color: white;
          font-size: 18rem;
          font-weight: bold;
        }
      }
      
      .game-content {
        flex: 1;
        display: flex;
        flex-direction: column;
        height: 52rem;
        justify-content: space-between;
        
        .game-header {
          display: flex;
          justify-content: space-between;
          align-items: center;
          
          .game-name {
            font-size: 16rem;
            font-weight: 600;
            line-height: 100%;
            color: #000;
          }
        }
          
        .game-desc {
          height: 32rem;
          display: flex;
          align-items: center;
          overflow: hidden;
          text-overflow: ellipsis;
          color: rgba(0, 0, 0, 0.45);
          font-family: inherit;
          font-size: 11rem;
          font-style: normal;
          font-weight: 400;
          line-height: 14rem;
          
          > div {
            display: -webkit-box;
            -webkit-line-clamp: 2;
            -webkit-box-orient: vertical;
            overflow: hidden;
            text-overflow: ellipsis;
            width: 100%;
            line-height: 14rem;
          }
        }
      }

      .game-status {
        display: none;
        min-width: 92rem;
        padding-top: 8rem;
        text-align: right;
        color: rgba(0, 0, 0, 0.45);
        font-size: 11rem;
      }

      .game-action {
        min-width: 92rem;
        padding-top: 8rem;
      }

      .play-btn {
        border-radius: 27rem;
        background: rgba(95, 64, 161, 0.1);
        color: #5F40A1;
        border: none;
        font-size: 12rem;
        line-height: 20rem;
        border-radius: 16rem;
        display: block;
        width: 92rem;
        text-align: center;
        padding: 4rem 0;
        font-weight: 600;
        cursor: pointer;
        transition: background 0.2s;
        white-space: nowrap;

        &:hover:not(.disabled) {
          background: #d1c4f6;
        }

        &.disabled {
          color: rgba(0, 0, 0, 0.45);
          cursor: default;
        }
      }
    }
  }
}

.home-page.desktop-mode {
  min-height: 100vh;
  padding-top: 68rem;
  background: linear-gradient(to bottom, #D5D6F7 0%, #E1E2F9 50%, #ffffff 100%);
}

.home-games {
  .loading-text {
    text-align: center;
    padding: 40rem 0;
    color: #666;
    font-size: 14rem;
  }
}

==================================================
FILE: src/ProjectL_frontend/src/pages2/HomePage/components/BannerSkeleton.jsx
==================================================

import React from "react";
import "./BannerSkeleton.scss";

const BannerSkeleton = ({ desktop = false }) => (
  <div className={`banner-skeleton${desktop ? " desktop-mode" : ""}`}>
    <div className="skeleton-banner" />
    <div className="skeleton-banner desktop-only" />
  </div>
);

export default BannerSkeleton;

==================================================
FILE: src/ProjectL_frontend/src/pages2/HomePage/components/BannerSkeleton.scss
==================================================

.banner-skeleton {
  margin: 0 16rem 20rem 16rem;

  .skeleton-banner {
    width: 100%;
    height: 160rem;
    border-radius: 12rem;
    overflow: hidden;
    background: linear-gradient(90deg, #f0f0f0 0%, #e8e8e8 20%, #f0f0f0 40%, #f0f0f0 100%);
    background-size: 200% 100%;
    animation: skeleton-shimmer 1.5s infinite linear;
  }

  .desktop-only {
    display: none;
  }

  &.desktop-mode {
    margin: 0 0 24rem 0;

    .skeleton-banner {
      height: 0;
      padding-bottom: 50%;
      border-radius: 16rem;
    }

    .desktop-only {
      display: block;
    }

    .desktop-only,
    .skeleton-banner {
      box-shadow: 0 12rem 24rem rgba(95, 64, 161, 0.12);
    }

    @media (min-width: 900px) {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 16rem;
    }
  }
}

@keyframes skeleton-shimmer {
  0% {
    background-position: 200% 0;
  }

  100% {
    background-position: -200% 0;
  }
}

==================================================
FILE: src/ProjectL_frontend/src/pages2/HomePage/components/DesktopBannerSection.jsx
==================================================

import React from "react";
import Slider from "react-slick";

const DesktopBannerSection = ({ banners, onBannerClick }) => {
  const onBannerKeyDown = (event, banner) => {
    if (event.key !== "Enter" && event.key !== " ") return;
    event.preventDefault();
    onBannerClick(banner);
  };

  const sliderSettings = {
    dots: true,
    infinite: banners.length > 2,
    speed: 500,
    slidesToShow: 2,
    slidesToScroll: 2,
    autoplay: banners.length > 1,
    autoplaySpeed: 5000,
    arrows: true,
    responsive: [
      {
        breakpoint: 900,
        settings: {
          slidesToShow: 2,
          slidesToScroll: 2,
        },
      },
      {
        breakpoint: 768,
        settings: {
          slidesToShow: 1,
          slidesToScroll: 1,
        },
      },
    ],
  };

  if (!banners?.length) return null;

  return (
    <div className="desktop-banner-section banner-carousel-web">
      <Slider {...sliderSettings}>
        {banners.map((banner, index) => (
          <div className="desktop-banner-slide" key={banner.id}>
            <div className="desktop-banner-card-wrap">
              <img
                className="desktop-banner-card"
                src={banner.imageUrl}
                alt={banner.title || "Randseed banner"}
                loading={index === 0 ? "eager" : "lazy"}
                fetchpriority={index === 0 ? "high" : "auto"}
                decoding="async"
                role={banner.linkUrl ? "button" : undefined}
                tabIndex={banner.linkUrl ? 0 : undefined}
                onKeyDown={(event) => onBannerKeyDown(event, banner)}
                style={{
                  cursor: banner.linkUrl ? "pointer" : "default",
                }}
                onClick={() => onBannerClick(banner)}
              />
            </div>
          </div>
        ))}
      </Slider>
    </div>
  );
};

export default DesktopBannerSection;

==================================================
FILE: src/ProjectL_frontend/src/pages2/HomePage/components/DesktopGameSection.jsx
==================================================

import React from "react";
import ProductLogo from "../../PagePlayV3/components/ProductLogo";

const DesktopGameSection = ({ title, games, onGameClick }) => {
  return (
    <div className="desktop-game-section">
      <div className="desktop-section-title">{title}</div>
      <div className="desktop-game-grid">
        {games.map((game) => {
          const isComingSoon = game.status === "coming_soon";

          return (
            <div
              className={`desktop-game-card${isComingSoon ? " coming-soon" : ""}`}
              key={game.id}
              onClick={() => {
                if (!isComingSoon) {
                  onGameClick(game);
                }
              }}
              style={{ cursor: isComingSoon ? "default" : "pointer" }}
            >
              <div className="desktop-game-logo">
                <ProductLogo src={game.logo} className="desktop-game-logo-img" />
              </div>
              <div className="desktop-game-content">
                <div className="desktop-game-name">{game.name}</div>
                <div className="desktop-game-desc">
                  {game.description || "No description available"}
                </div>
              </div>
            </div>
          );
        })}
      </div>
    </div>
  );
};

export default DesktopGameSection;

==================================================
FILE: src/ProjectL_frontend/src/pages2/HomePage/components/DesktopHomeContent.jsx
==================================================

import React from "react";
import DesktopBannerSection from "./DesktopBannerSection";
import DesktopGameSection from "./DesktopGameSection";
import BannerSkeleton from "./BannerSkeleton";
import GamesSkeleton from "./GamesSkeleton";
import "./desktop.scss";
import "./slick.css";

const DesktopHomeContent = ({
  banners,
  games,
  bannersLoading,
  gamesLoading,
  onBannerClick,
  onGameClick,
}) => {
  return (
    <div className="desktop-home-content">
      <div className="desktop-home-container">
        {bannersLoading ? (
          <BannerSkeleton desktop />
        ) : (
          <DesktopBannerSection
            banners={banners}
            onBannerClick={onBannerClick}
          />
        )}

        <div className="desktop-home-games">
          {gamesLoading ? (
            <>
              <GamesSkeleton title="Rand Game" desktop />
              <GamesSkeleton title="Rand Ball" desktop />
            </>
          ) : (
            games.map((game, index) => (
              <DesktopGameSection
                key={index}
                title={game[0]}
                games={game[1]}
                onGameClick={onGameClick}
              />
            ))
          )}
        </div>
      </div>
    </div>
  );
};

export default DesktopHomeContent;

==================================================
FILE: src/ProjectL_frontend/src/pages2/HomePage/components/GameSection.jsx
==================================================

import React from "react";
import { useNavigate } from "react-router-dom";
import ProductLogo from "../../PagePlayV3/components/ProductLogo";

const GameSection = ({ title, games }) => {
  const navigate = useNavigate();

  const handleGameClick = (game) => {
    if (game.status === "coming_soon") return;

    // 存储游戏信息到localStorage，避免闪烁
    localStorage.setItem('currentGameInfo', JSON.stringify({
      productId: game.id,
      productName: game.name,
      productLogo: game.logo,
      route: game.route
    }));

    if (game.frontend_route) {
      navigate({ pathname: game.frontend_route }, {
        state: {
          productId: game.id,
          productName: game.name,
          productLogo: game.logo
        }
      });
      return;
    }

    navigate({ pathname: game.frontend_route || game.route, search: `?pid=${game.id}` }, {
      state: {
        productId: game.id,
        productName: game.name,
        productLogo: game.logo
      }
    });
  };

  return (
    <div className="game-section">
      <div className="section-title">{title}</div>
      <div className="game-list">
        {games.map((game) => {
          return (
            <div
              className={`game-item${game.status === "coming_soon" ? " coming-soon" : " clickable"}`}
              key={game.id}
              onClick={() => handleGameClick(game)}
              style={{ cursor: game.status === "coming_soon" ? "default" : "pointer" }}
            >
              <div className="game-logo">
                <ProductLogo src={game.logo} className="game-logo-img" />
              </div>
              <div className="game-content">
                <div className="game-header">
                  <div className="game-name">{game.name}</div>
                </div>
                <div className="game-desc">
                  <div>{game.description || "No description available"}</div>
                </div>
              </div>
              <div className="game-action">
                {game.status === "coming_soon" ? (
                  <button className="play-btn disabled" disabled>
                    Coming soon
                  </button>
                ) : (
                  <button
                    className="play-btn"
                    onClick={(e) => {
                      e.stopPropagation();
                      handleGameClick(game);
                    }}
                  >
                    Play Now
                  </button>
                )}
              </div>
              {game.status === "coming_soon" ? (
                <div className="game-status">Coming soon</div>
              ) : null}
            </div>
          );
        })}
      </div>
    </div>
  );
};

export default GameSection;

==================================================
FILE: src/ProjectL_frontend/src/pages2/HomePage/components/GamesSkeleton.jsx
==================================================

import React from "react";
import "./GamesSkeleton.scss";

const GamesSkeleton = ({ title, desktop = false }) => (
  <div className={`games-skeleton${desktop ? " desktop-mode" : ""}`}>
    <div className="skeleton-section-title">{title}</div>
    <div className="skeleton-game-list">
      {Array.from({ length: 4 }).map((_, index) => (
        <div className="skeleton-game-item" key={index}>
          <div className="skeleton-game-logo"></div>
          <div className="skeleton-game-content">
            <div className="skeleton-game-name"></div>
            <div className="skeleton-game-desc"></div>
            <div className="skeleton-game-desc short"></div>
          </div>
          <div className="skeleton-play-btn"></div>
          <div className="skeleton-game-status"></div>
        </div>
      ))}
    </div>
  </div>
);

export default GamesSkeleton;

==================================================
FILE: src/ProjectL_frontend/src/pages2/HomePage/components/GamesSkeleton.scss
==================================================

.games-skeleton {
  margin: 0 16rem 24rem 16rem;

  &:first-of-type {
    margin-top: 20rem;
  }

  .skeleton-section-title {
    font-size: 18rem;
    font-weight: 600;
    margin: 0 0 10rem 0;
    color: #000;
  }

  .skeleton-game-list {
    display: flex;
    flex-direction: column;
    gap: 11rem;
  }

  .skeleton-game-item {
    display: flex;
    align-items: flex-start;
    gap: 12rem;

    .skeleton-game-logo {
      width: 52rem;
      height: 52rem;
      border-radius: 10rem;
      flex-shrink: 0;
      background: linear-gradient(90deg, #f0f0f0 0%, #e8e8e8 20%, #f0f0f0 40%, #f0f0f0 100%);
      background-size: 200% 100%;
      animation: skeleton-shimmer 1.5s infinite linear;
    }

    .skeleton-game-content {
      flex: 1;
      padding-top: 2rem;

      .skeleton-game-name,
      .skeleton-game-desc {
        border-radius: 6rem;
        background: linear-gradient(90deg, #f0f0f0 0%, #e8e8e8 20%, #f0f0f0 40%, #f0f0f0 100%);
        background-size: 200% 100%;
        animation: skeleton-shimmer 1.5s infinite linear;
      }

      .skeleton-game-name {
        width: 120rem;
        height: 16rem;
        margin-bottom: 8rem;
      }

      .skeleton-game-desc {
        width: 100%;
        height: 12rem;
        margin-bottom: 6rem;

        &.short {
          width: 75%;
          margin-bottom: 0;
        }
      }
    }

    .skeleton-play-btn,
    .skeleton-game-status {
      background: linear-gradient(90deg, #f0f0f0 0%, #e8e8e8 20%, #f0f0f0 40%, #f0f0f0 100%);
      background-size: 200% 100%;
      animation: skeleton-shimmer 1.5s infinite linear;
      flex-shrink: 0;
    }

    .skeleton-play-btn {
      width: 92rem;
      height: 28rem;
      border-radius: 16rem;
      margin-top: 8rem;
    }

    .skeleton-game-status {
      display: none;
      width: 92rem;
      height: 12rem;
      border-radius: 8rem;
      margin-top: 8rem;
    }
  }

  &.desktop-mode {
    margin: 0 0 24rem 0;

    .skeleton-section-title {
      font-size: 24rem;
      margin: 0 0 16rem 0;
    }

    .skeleton-game-list {
      display: grid;
      grid-template-columns: repeat(4, minmax(0, 1fr));
      gap: 16rem;
    }

    .skeleton-game-item {
      align-items: center;
      flex-direction: column;
      gap: 10rem;
      border-radius: 12rem;
      padding: 14rem;

      .skeleton-game-logo {
        width: 86rem;
        height: 86rem;
        border-radius: 12rem;
      }

      .skeleton-game-content {
        width: 100%;
        padding-top: 0;

        .skeleton-game-name {
          width: 70%;
          height: 20rem;
          margin: 0 auto 10rem auto;
        }

        .skeleton-game-desc {
          width: 100%;
          height: 14rem;
          margin-bottom: 8rem;

          &.short {
            width: 80%;
            margin: 0 auto;
          }
        }
      }

      .skeleton-play-btn,
      .skeleton-game-status {
        display: none;
      }
    }
  }
}

@keyframes skeleton-shimmer {
  0% {
    background-position: 200% 0;
  }

  100% {
    background-position: -200% 0;
  }
}

==================================================
FILE: src/ProjectL_frontend/src/pages2/HomePage/components/desktop.scss
==================================================

.desktop-home-content {
  min-height: calc(100vh - 68rem);
  padding: 12rem 0 60rem;
  font-family: var(--app-font-family);
}

.desktop-home-container {
  max-width: 1024rem;
  margin: 0 auto;
  padding: 0 16rem;
}

.desktop-banner-section {
  padding: 0 0 24rem;

  .desktop-banner-card-wrap {
    width: 100%;
    padding: 0 8rem;
  }

  .desktop-banner-slide {
    padding-bottom: 8rem;
  }

  .desktop-banner-card {
    width: 100%;
    aspect-ratio: 2 / 1;
    border-radius: 16rem;
    display: block;
    object-fit: cover;
    box-shadow: 0 12rem 30rem rgba(95, 64, 161, 0.18);
    transition: transform 0.3s ease, box-shadow 0.3s ease;

    &:hover {
      transform: scale(1.02);
      box-shadow: 0 16rem 36rem rgba(95, 64, 161, 0.28);
    }
  }
}

.desktop-home-games {
  display: flex;
  flex-direction: column;
  gap: 8rem;
}

.desktop-game-section {
  .desktop-section-title {
    margin-bottom: 6rem;
    color: #000;
    font-size: 24rem;
    font-weight: 600;
    line-height: 1.2;
  }

  .desktop-game-grid {
    display: grid;
    grid-template-columns: repeat(4, minmax(0, 1fr));
    gap: 8rem;
  }

  .desktop-game-card {
    display: flex;
    flex-direction: column;
    align-items: center;
    gap: 10rem;
    border-radius: 12rem;
    padding: 6rem;
    transition: transform 0.3s ease, box-shadow 0.3s ease, opacity 0.3s ease;

    &:hover {
      transform: translateY(-4rem);
      box-shadow: 0 16rem 24rem rgba(0, 0, 0, 0.08);
    }

    &.coming-soon {
      opacity: 0.65;

      &:hover {
        transform: none;
        box-shadow: none;
      }
    }
  }

  .desktop-game-logo {
    flex-shrink: 0;
    width: 70rem;
    height: 70rem;
    border-radius: 10rem;
    overflow: hidden;
    box-shadow: 0 2rem 8rem rgba(0, 0, 0, 0.08);
  }

  .desktop-game-logo-img {
    width: 100%;
    height: 100%;
    border-radius: 10rem;
    object-fit: cover;
  }

  .desktop-game-content {
    width: 100%;
    text-align: center;
  }

  .desktop-game-name {
    margin-bottom: 2rem;
    color: #000;
    font-size: 20rem;
    font-weight: 400;
    line-height: 1.25;
  }

  .desktop-game-desc {
    color: rgba(0, 0, 0, 0.8);
    font-size: 14rem;
    font-weight: 400;
    line-height: 1.45;
    display: -webkit-box;
    -webkit-line-clamp: 2;
    -webkit-box-orient: vertical;
    overflow: hidden;
    text-overflow: ellipsis;
  }
}

@media (max-width: 1024px) {
  .desktop-game-section {
    .desktop-game-grid {
      grid-template-columns: repeat(3, minmax(0, 1fr));
    }
  }
}

@media (max-width: 900px) {
  .desktop-game-section {
    .desktop-game-grid {
      grid-template-columns: repeat(2, minmax(0, 1fr));
    }
  }
}

==================================================
FILE: src/ProjectL_frontend/src/pages2/HomePage/components/slick.css
==================================================

@import "slick-carousel/slick/slick.css";
@import "slick-carousel/slick/slick-theme.css";

.banner-carousel-web .slick-list {
  overflow: hidden;
}

.banner-carousel-web .slick-track {
  display: flex;
}

.banner-carousel-web .slick-slide > div {
  height: 100%;
}

.banner-carousel-web .slick-prev,
.banner-carousel-web .slick-next {
  display: block !important;
  width: 40rem;
  height: 40rem;
  z-index: 10;
}

.banner-carousel-web .slick-prev {
  left: -20rem;
}

.banner-carousel-web .slick-next {
  right: -20rem;
}

.banner-carousel-web .slick-prev:before,
.banner-carousel-web .slick-next:before {
  font-size: 40rem;
  color: rgba(95, 64, 161, 0.8);
  opacity: 1;
}

.banner-carousel-web .slick-prev:hover:before,
.banner-carousel-web .slick-next:hover:before {
  color: rgba(95, 64, 161, 1);
}

.banner-carousel-web .slick-dots {
  bottom: -30rem !important;
}

.banner-carousel-web .slick-dots li button:before {
  color: rgba(95, 64, 161, 0.5) !important;
  font-size: 10rem !important;
  opacity: 1 !important;
}

.banner-carousel-web .slick-dots li.slick-active button:before {
  color: rgba(95, 64, 161, 1) !important;
}

==================================================
FILE: src/ProjectL_frontend/src/pages2/HomePage/imgs/Vector.svg
==================================================

<svg width="14" height="16" viewBox="0 0 14 16" fill="none" xmlns="http://www.w3.org/2000/svg">
<path id="Vector" d="M7 0.965576C3.13406 0.965576 0 4.11508 0 8.00006C0 11.885 3.13406 15.0345 7 15.0345C10.8659 15.0345 14 11.885 14 8.00006C14 4.11508 10.8659 0.965576 7 0.965576ZM6.5 4.95827C5.8[...] 
</svg>